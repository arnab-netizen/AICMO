# Streamlit Secrets Configuration
# This file is used for development and testing
# On Render, set these as environment variables instead

# Backend URL for AICMO API
# Must point to the deployed backend (e.g., Render backend)
AICMO_BACKEND_URL = "https://aicmo-backend.onrender.com"

# Note: OPENAI_API_KEY is not stored here
# It's read from environment variables for security
# On local: set in .env file
# On Render: set as OPENAI_API_KEY secret
